'use strict';

angular.module('home', [
        'ngCookies',
        'packages'
])
    .controller(
        'HomeController',
        [
            '$scope',
            function (
                $scope) {


            }
        ]);